package com.linc.audiowaveform.sample.util

import androidx.compose.runtime.Composable
import androidx.compose.ui.graphics.Brush
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.graphics.SolidColor
import androidx.compose.ui.unit.dp
import com.linc.audiowaveform.infiniteHorizontalGradient
import com.linc.audiowaveform.infiniteLinearGradient
import com.linc.audiowaveform.infiniteVerticalGradient
import com.linc.audiowaveform.sample.model.ColorPalette




/*
object ColorPaletteHolder {

    private var selected: Int = 0

    @Composable
    fun setPalette(palette: ColorPalette) {
        selected = getMockPalettes().indexOf(palette)
    }

    @Composable
    fun getPalette() = getMockPalettes().get(selected)

    @JvmName("getPalettesFun")
    @Composable
    fun getMockPalettes() = listOf(
        ColorPalette(SolidColor(Color.Cyan), SolidColor(Color.LightGray)),
        ColorPalette(SolidColor(Color.Blue), SolidColor(Color.Yellow)),
        ColorPalette(SolidColor(Color.White), SolidColor(Color.Red)),
        ColorPalette(
            Brush.horizontalGradient(listOf(Color(0xFF1A2980), Color(0xFF26D0CE))),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            Brush.verticalGradient(listOf(Color(0xFF403B4A), Color(0xFFE7E9BB))),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            Brush.linearGradient(listOf(Color(0xFFDA22FF), Color(0xFF9733EE))),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            Brush.infiniteHorizontalGradient(
                colors = listOf(Color(0xFFFC8B6A), Color(0xFFE10467)),
                width = 192.dp.value
            ),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            Brush.infiniteVerticalGradient(
                colors = listOf(Color(0xFFFFE000), Color(0xFF799F0C)),
                width = 96.dp.value
            ),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            Brush.infiniteLinearGradient(
                colors = listOf(Color(0xFFA18CD1), Color(0xFFFBC2EB)),
                width = 48.dp.value
            ),
            SolidColor(Color.LightGray)
        ),
    )

    */
/*@JvmName("getPalettesFun")
    @Composable
    fun getMockPalettes() = listOf(
        ColorPalette(UUID.randomUUID().toString(), SolidColor(Color.Cyan), SolidColor(Color.LightGray)),
        ColorPalette(UUID.randomUUID().toString(), SolidColor(Color.Blue), SolidColor(Color.Yellow)),
        ColorPalette(UUID.randomUUID().toString(), SolidColor(Color.White), SolidColor(Color.Red)),
        ColorPalette(
            UUID.randomUUID().toString(),
            Brush.horizontalGradient(listOf(Color(0xFF1A2980), Color(0xFF26D0CE))),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            UUID.randomUUID().toString(),
            Brush.verticalGradient(listOf(Color(0xFF403B4A), Color(0xFFE7E9BB))),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            UUID.randomUUID().toString(),
            Brush.linearGradient(listOf(Color(0xFFDA22FF), Color(0xFF9733EE))),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            UUID.randomUUID().toString(),
            Brush.infiniteHorizontalGradient(
                colors = listOf(Color(0xFFFC8B6A), Color(0xFFE10467)),
                width = 192.dp.value
            ),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            UUID.randomUUID().toString(),
            Brush.infiniteVerticalGradient(
                colors = listOf(Color(0xFFFFE000), Color(0xFF799F0C)),
                width = 96.dp.value
            ),
            SolidColor(Color.LightGray)
        ),
        ColorPalette(
            UUID.randomUUID().toString(),
            Brush.infiniteLinearGradient(
                colors = listOf(Color(0xFFA18CD1), Color(0xFFFBC2EB)),
                width = 48.dp.value
            ),
            SolidColor(Color.LightGray)
        ),
    )*//*


}*/
